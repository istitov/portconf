EXTRA_DIST = portconf _portconf

if BASH_COMPLETION
bashcompletion_DATA = portconf
bashcompletiondir = $(datadir)/bash-completion/completions
endif

if ZSH_COMPLETION
zshcompletion_DATA = _portconf
zshcompletiondir = $(datadir)/zsh/site-functions
endif
