# shellcheck configuration for the portconf source tree.
# https://github.com/koalaman/shellcheck/wiki/Directive
#
# CI enforces `shellcheck --severity=error src/portconf` — so warnings
# and below do not block builds.  This file is for the developer
# running `shellcheck src/portconf` locally without the --severity flag
# and wanting a low-noise baseline.

# SC2010 (`Don't use ls | grep`): three sites in src/portconf.in
# (repo_fix, f_to_d, d_to_f) match this pattern.  Each one needs a
# functional rewrite (find / for-glob with a -name condition) — not a
# suppression.  They are tracked as a 2.1.0 line item; until then,
# silence the rule so it doesn't drown out new findings.
disable=SC2010
