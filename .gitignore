# autotools generated
/aclocal.m4
/autom4te.cache/
/config.log
/config.status
/configure
/install-sh
/missing
/Makefile
/Makefile.in
/src/Makefile
/src/Makefile.in
/src/portconf
/man/Makefile
/man/Makefile.in
/completion/Makefile
/completion/Makefile.in
/packaging/Makefile
/packaging/Makefile.in

# dist tarballs
/*.tar.*
/*.tar

# editor
*~
*.swp
*.swo

# local-only tooling — verify-local.sh runs the property invariants against
# the maintainer's REAL /etc/portage; kept untracked so private config data
# can never leak into the repo (the committed property suite uses synthetic
# fixtures only).
/scripts/verify-local.sh
